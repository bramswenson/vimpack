#!/usr/bin/env ruby

require 'rubygems'
require 'bundler/setup'
begin
  require 'vimpack'
rescue LoadError => e
  $LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))
  require 'vimpack'
end
Vimpack::CLI.start
